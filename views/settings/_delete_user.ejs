<!-- Form to cancel user application -->
<div id="cancel_account_modal" class="modal in" style="top: 0px;" aria-hidden="false">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <a href="#" class="close" data-dismiss="modal">×</a>
                <h3>Cancel account</h3>
            </div>
            <form id="cancel_account_modal" ng-controller="DummyCtrl" name="" autocomplete="" class="" action="/settings/cancel?_method=delete" method="POST" enctype="">
                <input type='hidden' name='_csrf' value='<%= csrfToken%>' />
                <div class="modal-body clearfix">
                    <div class="left">
                        <fieldset>
                        </fieldset>
                    </div>
                    <div class="right">
                        <p>Are you sure? Cancelling your account implies the deletion of all your FIWARE Lab resources (including deployed resources in the Cloud infrastructure). This action is permanent.</p>
                    </div>
                </div>
                <div class="modal-footer">
                    <input type="submit" class="btn btn-primary" value="Confirm">
                    <button data-dismiss="modal" class="btn btn-default secondary cancel">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>